<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.music.mapper.TrackMapper">
		<sql id="search_box">
			<trim prefix="(" suffix=") AND " prefixOverrides="OR">
				<if test="keyword != null">
					a.name like '%'||#{keyword}||'%'
				</if>
			</trim>
		</sql>
  	
	<select id="listNewly" resultType="com.music.domain.AlbumVO">
		<![CDATA[
			select * from newly order by tbno desc
		]]>
	</select>

	<select id="readTrack" resultType="com.music.domain.TrackVO">
		select * from track_m where abno = #{abno}
	</select>
	
	<select id="selectTrack" resultType="com.music.domain.TrackVO">
		select * from track_m where tbno = #{tbno}
	</select>
	
	
	<!--   	어드민 -->
  	<select id="viewTrackListWithPaging" resultType="com.music.domain.TrackVO">
		<![CDATA[
			select * from 
		( 
			select /*+ index_desc(track_m track_m_pk) */ rownum rn, tbno, abno, name, genre, detail, price, songname, songrealname, pbno from track_m 
			where 
		]]>
		<include refid="search_box"></include>
		<![CDATA[			
					rownum <= #{amount} * #{pageNum}  order by abno desc
				) 
			where rn > #{amount} * (#{pageNum}-1)
		]]>
	</select>
	
	<select id="countTrack" resultType="int">
			select count(*) from track_m a where 
			<include refid="search_box"></include>
			tbno > 0
	</select>
	
	<update id="updateTrack">
		update track_m set name=#{name}, genre=#{genre}, detail=#{detail}, price=#{price}, songname=#{songname}, songrealname=#{songrealname} 
		where abno = #{abno}
	</update>
	
</mapper>

