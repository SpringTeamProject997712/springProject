<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.music.mapper.AlbumMapper">

  	<insert id="insertAlbum">
  		<selectKey keyProperty="pbno" order="BEFORE" resultType="int">
  			select product_m_seq.nextval from dual
  		</selectKey>
  		insert into product_m (pbno, name, kind, image, detail, price1, price2, releasedate, labelnum, singer, onlines, stock, songname, songrealname) 
		values (#{pbno},#{name},#{kind},#{iamge},#{detail},#{price1},#{price2},#{releasedate},#{labelnum}, 
		#{singer},#{onlines},#{stock},#{songname},#{songrealname})
  	</insert>
  	
  	<select id="listAlbum" resultType="com.music.domain.AlbumVO">
		<![CDATA[
			select /*+ index_desc(product_m product_m_pk) */ * from product_m where pbno > 0
		]]>
	</select>

	<select id="readAlbum" resultType="com.music.domain.AlbumVO">
		select * from product_m where pbno = #{pbno}
	</select>
  	
</mapper>
